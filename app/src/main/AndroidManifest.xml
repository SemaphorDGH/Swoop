<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="semaphor.swoop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="swoop"
        android:theme="@style/AppTheme">
        <activity android:name="semaphor.swoop.activities.LoginActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="semaphor.swoop.activities.SignupActivity" android:theme="@style/AppTheme.Dark" />   `
        <activity android:name=".activities.HomeActivity" />
        <!--
        <activity android:name=".activities.HomeActivity" android:noHistory="true"/>
        <activity android:name=".database.SqliteTest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
    </application>

</manifest>